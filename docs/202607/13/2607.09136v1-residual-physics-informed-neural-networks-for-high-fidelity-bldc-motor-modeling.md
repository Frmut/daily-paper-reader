---
title: Residual Physics-Informed Neural Networks for High-Fidelity BLDC Motor Modeling
title_zh: 用于高保真无刷直流电机建模的残差物理信息神经网络
authors: Haitham El-Hussieny
date: 2026-07-10
pdf: "https://arxiv.org/pdf/2607.09136v1"
tags: ["query:pmsm"]
score: 8.0
evidence: 三相无刷直流电机动力学的高保真建模
tldr: 本研究针对机器人关节控制中的无刷直流（BLDC）电机建模问题，提出了一种基于残差网络（ResNet）架构的物理信息神经网络（PINN）。该模型通过融合机电与热力学常微分方程（ODE）的复合损失函数，能够准确预测电机转角、转速、三相电流及绕组温度等六个状态变量。该方法采用课程调度策略优化训练，在保持高精度的同时，推理速度比传统ODE求解器快118倍，为实时控制提供了高效的代理模型。
selection_source: fresh_fetch
motivation: 高性能机器人关节控制需要精确的无刷直流（BLDC）电机动力学模型以实现高精度控制。
method: 提出一种结合ResNet架构与物理约束（机电及热力学方程）的PINN模型，并采用课程调度策略逐步激活物理损失项。
result: 该模型在标准CPU上训练仅需不到两分钟，推理速度比传统ODE求解器快达118倍，延迟低至微秒级。
conclusion: 该高保真且实时的PINN代理模型为机器人电机的实时观测与控制提供了一种极具潜力的解决方案。
---

## 摘要
无刷直流 (BLDC) 电机的精确动力学建模是高性能机器人关节控制的基础。本文提出了一种具有深度残差 (ResNet) 骨干结构的物理信息神经网络 (PINN)，用于学习完整六状态 BLDC 电机动力学的连续时间代理模型。以仿真时间、施加的三相电压和励磁参数作为输入，该网络直接预测所有电机状态变量——包括转子角度、角速度、三相电流和绕组温度——同时通过复合物理-数据损失函数满足控制机电和热力学的常微分方程 (ODE)。采用课程调度策略逐步激活物理惩罚项，以防止过早收敛。在标准 CPU 上，训练过程可在两分钟内完成。至关重要的是，一旦完成训练，PINN 推理的单次查询延迟仅为 0.1-22 μs，比传统的 ODE 求解器快达 118 倍，使其适用于实时观测器和控制应用。

## Abstract
Accurate dynamics modeling of Brushless DC (BLDC) motors is fundamental to high-performance robotic joint control. This paper presents a Physics-Informed Neural Network (PINN) with a deep residual (ResNet) backbone that learns a continuous-time surrogate of the full six-state BLDC motor dynamics. Given simulation time, applied three-phase voltages, and excitation parameters as inputs, the network directly predicts all motor state variables -- rotor angle, angular velocity, three-phase currents, and winding temperature -- while simultaneously satisfying the governing electromechanical and thermal ODEs through a composite physics-data loss. A curriculum scheduling strategy gradually activates the physics penalty to prevent premature convergence. Training runs are completed in under two minutes on a standard CPU. Crucially, once trained, PINN inference achieves latencies of 0.1--22, mu s per query, up to 118x faster than conventional ODE solvers, making it suitable for real-time observer and control applications.

---

## 论文详细总结（自动生成）

以下是对论文《Residual Physics-Informed Neural Networks for High-Fidelity BLDC Motor Modeling》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在保证物理一致性的前提下，为无刷直流（BLDC）电机建立一个既高保真又具备实时推理能力的动力学代理模型。
*   **研究动机**：
    *   **传统模型局限**：基于一阶原理的“白盒”模型（ODE）虽然可解释，但对参数识别精度要求极高；而纯数据驱动的“黑盒”神经网络在训练分布外容易违反物理定律。
    *   **计算效率需求**：高性能机器人控制（如MPC）要求模型在毫秒级内响应，传统数值求解器（如RK45）在复杂多物理场耦合下难以满足实时性。
    *   **技术挑战**：标准PINN在处理刚性方程或深层网络时存在梯度消失和频谱偏差问题。

### 2. 方法论
*   **核心思想**：结合**残差网络（ResNet）**架构与**物理信息神经网络（PINN）**，将电机的机电和热力学控制方程嵌入损失函数中。
*   **关键技术细节**：
    *   **输入与输出**：输入为时间、频率、幅值及三相电压 $(t, f, A, V_a, V_b, V_c)$；输出为六维状态向量 $[\theta, \omega, I_a, I_b, I_c, T]$（角度、角速度、三相电流、温度）。
    *   **残差架构**：采用包含线性投影层和两个残差块（ResBlocks）的紧凑结构（仅约1300个参数），利用跳跃连接确保梯度在物理约束优化过程中的流动。
    *   **复合损失函数**：
        *   **数据损失 ($L_{data}$)**：预测值与仿真真值之间的归一化均方误差。
        *   **物理损失 ($L_{phys}$)**：利用自动微分（Autograd）计算网络输出对时间的导数，并与由电机方程（反电动势、电磁转矩、热动力学等）定义的ODE残差进行对比。
    *   **课程调度策略（Curriculum Scheduling）**：设置预热期和线性增长期，让物理惩罚项 $\lambda_p$ 逐渐激活，防止物理约束在训练初期干扰数据拟合。

### 3. 实验设计
*   **数据集**：通过显式欧拉法生成10条轨迹（每条1秒，采样率1ms），共10,000个样本。励磁信号为随机采样的正弦电压（5-20Hz, 5-15V）。
*   **Benchmark（基准）**：
    *   **显式欧拉法（Explicit-Euler）**：常规数值积分器。
    *   **RK45（Runge-Kutta）**：Scipy提供的自适应步长高精度求解器。
*   **对比维度**：预测精度（RMSE/NRMSE）、推理延迟（Latency）、吞吐量（Throughput）。

### 4. 资源与算力
*   **算力设备**：实验明确指出在**标准 CPU** 上运行。
*   **训练时长**：总训练时间**少于 2 分钟**（约110秒）。
*   **模型规模**：极其轻量化，仅1,302个可训练参数。

### 5. 实验数量与充分性
*   **实验规模**：
    *   使用了8,000个训练点和2,000个验证点。
    *   在未见过的测试轨迹（8Hz, 9V）上进行了泛化性验证。
    *   针对不同批量大小（1到10,000个点）进行了200次重复的延迟基准测试。
*   **充分性评价**：实验设计较为客观，涵盖了从训练动力学分析到实时性能评估的完整流程。虽然数据集规模较小，但对于验证“代理模型”这一概念的有效性是充分的。

### 6. 主要结论与发现
*   **推理速度极快**：PINN的单次查询延迟为0.1–22 μs。在大批量（10,000点）并行计算时，速度比RK45快**118倍**，比欧拉法快39倍。
*   **物理一致性**：通过损失曲线观察到，物理损失在引入后主动下降，证明了Autograd成功将物理约束传导至网络权重。
*   **精度表现**：模型能准确捕捉所有六个状态变量的波形趋势。慢变状态（角度、温度）的归一化误差（NRMSE）小于14%，而快变电流状态误差约31%。

### 7. 优点
*   **实时性卓越**：连续时间参数化允许在任意时间点 $t$ 进行 $O(1)$ 复杂度的查询，无需顺序迭代，极适合实时控制。
*   **训练高效**：课程调度策略解决了PINN常见的训练不稳定问题，且对硬件要求极低（CPU即可快速训练）。
*   **架构优化**：引入ResNet有效缓解了深层PINN的梯度传播问题。

### 8. 不足与局限
*   **电流预测精度有限**：对于快速变化的电流信号，31%的NRMSE相对较高，可能不足以直接用于高性能电流环控制。
*   **场景单一**：实验仅基于正弦电压励磁，未考虑PWM方波驱动、负载扰动或复杂的换向逻辑（如Hall传感器切换）。
*   **参数依赖**：物理损失的计算仍依赖于预先定义的电机参数（如 $R, L, J$），若实际电机参数发生剧烈漂移，物理约束的准确性会受影响。

（完）
